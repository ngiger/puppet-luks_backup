# run once rake spec_prep
# run often (avoiding download) rake spec_standalone
fixtures:
  forge_modules:
      stdlib:
          repo: "puppetlabs/stdlib"
          ref: "4.3.2"
      apt: "puppetlabs/apt"
  symlinks:
    luks_backup: "#{source_dir}"
